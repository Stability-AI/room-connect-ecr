apiVersion: v1
kind: Service
metadata:
  name: room-connect-ecr
  labels:
    app: room-connect-ecr
spec:
  type: ClusterIP
  selector:
    app: room-connect-ecr
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
